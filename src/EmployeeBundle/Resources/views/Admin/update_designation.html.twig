{% extends "EmployeeBundle:layout:layout-admin.html.twig" %}


{% block body %}
<div class="row top-container">
	<div class=" top-header">
			Update Designations
		</div>
		<div class="col col-md-12 admin-top-support">
			<button class="button-primary add-new-designation">Add New</button>
		</div>
		<div class="col col-md-12 table-container">
			<table class="display-assets-table" id="displayTable">
			<thead>
				<tr>
				<th>sr. no</th>
				<th>Designation Name</th>
				
				<th>Action</th>
			</tr>
			</thead>
			<tbody>
			{% set count = 0 %}
			{% for designation in designations %}
				{% set count = count + 1 %}
       			 <tr>
       			 	<td>{{ count }}</td>
       			 	<td>{{ designation.name }}</td>
       			 	<td><button class="button-form-type change-department"  data-id="{{designation.id}}">update</button> <button data-toggle="modal" data-target="#delete"   data-id="{{designation.id}}" class="button-form-type-danger delete-department">Delete</button></td>
       			 	
       			 </tr>
   			 {% endfor %}
			
			</tbody>
		</table>

			<!-- Reject Modal -->	
			 	<div id="delete" class="modal fade" role="dialog">
			  <div class="modal-dialog">

			    <!-- Modal content-->
			    <div class="modal-content">
			      <div class="modal-header">
			        <button type="button" class="close" data-dismiss="modal">&times;</button>
			        <h4 class="modal-title">Delete Department</h4>
			      </div>
			      <div class="modal-body">
			        <p>This Department will be Deleted.</p>
			      </div>
			      <div class="modal-footer">
			      	<button type="button" class="btn button-primary delete-department-confirm" data-dismiss="modal">Delete</button>
			        <button type="button" class="btn button-primary-danger " data-dismiss="modal">close</button>
			      </div>
			    </div>

			  </div>
			</div>
	</div>
</div>
{% endblock %}
{% block javascripts %}


<script type="text/javascript">
	$(document).ready(function () {
		$('.add-new-designation').click(function(){
				window.location = location.origin + "/admin/add-new-designation"
			});
		
	});
	</script>
{% endblock %}


